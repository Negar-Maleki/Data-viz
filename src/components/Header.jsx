import styled from "styled-components";

const StyledHeader = styled.div`
  font-size: 2em;
  padding-left: 1em;
`;

function Header() {
  return <StyledHeader>Data viz</StyledHeader>;
}

export default Header;
